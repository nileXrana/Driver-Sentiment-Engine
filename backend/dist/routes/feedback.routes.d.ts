/**
 * feedback.routes.ts
 * -------------------
 * Route definitions for feedback-related endpoints.
 * Separating routes from controllers keeps things clean and testable.
 */
import { Router } from "express";
import { FeedbackController } from "../controllers/FeedbackController";
export declare function createFeedbackRoutes(feedbackController: FeedbackController): Router;
//# sourceMappingURL=feedback.routes.d.ts.map